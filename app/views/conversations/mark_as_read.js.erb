$("#conversation_link_<%= @conversation.id.to_s %>").animate({ opacity: 0 }).parent().parent().removeClass('unread');
$this = $('#activity > .badge');
$this.text( <%= @unread_notifications_count %> );
if ( $this.text() == 0 ) {
  $('#activity >.badge').fadeOut(1000);
}
